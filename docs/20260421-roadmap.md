# Roadmap

## Benchmark Data Enrichment

**Status:** Pending  
**Priority:** Medium

The `/api/benchmarks/models` endpoint is now live and returns benchmark data from the database. Currently only `docling-extract` on CORD (3 samples, 75.34% accuracy) has results.

### TODO
- [ ] Run benchmarks against all provider/model combos listed in `backend/config/providers.yaml`
- [ ] Add benchmark accuracy badges to the model selector dropdown (frontend wiring exists, needs data)
- [ ] Consider adding latency and cost comparison columns
- [ ] Automate benchmark runs via CI or scheduled job
- [ ] Expand beyond CORD dataset to FUNSD, synthetic_invoice
