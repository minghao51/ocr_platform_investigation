version: '3.8'

services:
  app:
    build: .
    container_name: ocr_platform
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./backend:/app/backend
    env_file:
      - .env
    restart: unless-stopped
